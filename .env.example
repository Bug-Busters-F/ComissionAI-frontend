# Configuração da API do Spring Boot
# Com o proxy configurado no vite.config.js, as chamadas para /api/v1 são redirecionadas
# para http://localhost:8080/api/v1 sem problemas de CORS no navegador.
VITE_API_URL=/api/v1
